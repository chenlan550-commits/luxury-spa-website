# 香熏緻身心調理館 - 後端搭建完整方案

**方案日期:** 2025年10月12日  
**技術架構:** Firebase + React + Cloudflare + GitHub

---

## 📋 系統架構總覽

### 整體架構
```
前端網站 (React)
    ↓
Firebase Services
    ├─ Authentication (用戶認證)
    ├─ Firestore Database (預約資料庫)
    ├─ Storage (圖片儲存)
    └─ Hosting (可選)
    ↓
管理後台 (React Admin)
    ↓
Cloudflare (CDN + DNS)
    ↓
域名: polung.dpdns.org
```

### 核心功能模組

#### 1. 前端網站功能
- ✅ 線上預約表單
- ✅ 預約確認通知
- ✅ 預約查詢功能
- ✅ 服務項目展示
- ✅ 聯絡表單

#### 2. 管理後台功能
- ✅ 預約管理（查看、編輯、刪除、狀態更新）
- ✅ 客戶資料管理
- ✅ 療程服務管理
- ✅ 營業時間設定
- ✅ 統計報表
- ✅ 管理員登入/權限控制

---

## 🔧 技術選型說明

### Firebase Services

#### Firestore Database
**用途:** 儲存預約資料、客戶資料、療程資料

**資料結構設計:**
```javascript
// 預約資料 (appointments)
{
  id: "auto-generated",
  customerName: "客戶姓名",
  phone: "0912345678",
  email: "customer@example.com",
  serviceId: "B01",
  serviceName: "顱沐淋巴舒壓",
  date: "2025-10-15",
  time: "14:00",
  duration: 90,
  price: 2200,
  status: "pending", // pending, confirmed, completed, cancelled
  notes: "客戶備註",
  createdAt: timestamp,
  updatedAt: timestamp
}

// 療程服務 (services)
{
  id: "B01",
  name: "顱沐淋巴舒壓",
  category: "body",
  duration: 90,
  price: 2200,
  discountPrice: 1600,
  description: "...",
  available: true
}

// 營業設定 (settings)
{
  businessHours: {
    monday: { open: "10:00", close: "21:00" },
    tuesday: { open: "10:00", close: "21:00" },
    // ...
  },
  holidays: ["2025-01-01", "2025-02-10"],
  bookingInterval: 30, // 分鐘
  advanceBookingDays: 30 // 可預約天數
}
```

#### Firebase Authentication
**用途:** 管理後台登入認證

**認證方式:**
- Email/Password 認證
- 管理員帳號設定

#### Firebase Storage (可選)
**用途:** 儲存療程圖片、客戶上傳檔案

---

## 🚀 實施步驟

### 階段一: Firebase 項目設置 (30分鐘)

#### 1. 創建 Firebase 項目
1. 前往 [Firebase Console](https://console.firebase.google.com/)
2. 點擊「新增專案」
3. 專案名稱: `spa-aromatherapy` 或自訂
4. 啟用 Google Analytics (可選)

#### 2. 啟用 Firebase 服務
- ✅ **Firestore Database**
  - 選擇「生產模式」
  - 地區選擇: asia-east1 (台灣)
  
- ✅ **Authentication**
  - 啟用「電子郵件/密碼」登入方式
  - 創建第一個管理員帳號

- ✅ **Storage** (可選)
  - 啟用儲存服務
  - 設定安全規則

#### 3. 獲取 Firebase 配置
```javascript
// 在 Firebase Console > 專案設定 > 一般 > 您的應用程式
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

---

### 階段二: 前端網站整合 Firebase (2-3小時)

#### 1. 安裝 Firebase SDK
```bash
cd /home/ubuntu/luxury-spa-website
pnpm install firebase
```

#### 2. 創建 Firebase 配置文件
檔案: `src/firebase/config.js`

#### 3. 創建預約表單組件
檔案: `src/components/BookingForm.jsx`

功能:
- 選擇療程服務
- 選擇日期時間
- 填寫客戶資訊
- 提交到 Firestore
- 顯示確認訊息

#### 4. 創建預約查詢功能
檔案: `src/components/BookingLookup.jsx`

功能:
- 輸入預約編號或手機號碼
- 查詢預約狀態
- 取消預約功能

---

### 階段三: 管理後台開發 (4-6小時)

#### 1. 創建管理後台項目
```bash
cd /home/ubuntu
pnpm create vite spa-admin --template react
cd spa-admin
pnpm install
pnpm install firebase react-admin ra-data-fakerest
```

#### 2. 安裝 React Admin
React Admin 是一個強大的管理後台框架,提供:
- 📊 自動生成 CRUD 介面
- 🔍 搜尋和篩選功能
- 📱 響應式設計
- 🔐 內建認證系統
- 📈 儀表板和統計

#### 3. 創建 Firebase Data Provider
連接 React Admin 與 Firebase Firestore

#### 4. 創建管理頁面
- **預約管理頁面** - 列表、編輯、詳情
- **客戶管理頁面** - 客戶資料管理
- **療程管理頁面** - 服務項目管理
- **儀表板** - 統計數據展示

#### 5. 設定權限控制
- 管理員登入頁面
- 權限驗證
- 登出功能

---

### 階段四: Cloudflare 配置 (30分鐘)

#### 1. 前端網站部署到 Cloudflare Pages

**方式一: 透過 GitHub 自動部署**
1. 將網站推送到 GitHub repository
2. 在 Cloudflare Pages 連接 GitHub repo
3. 設定構建命令: `npm run build`
4. 設定輸出目錄: `dist`
5. 自動部署

**方式二: 使用 Wrangler CLI**
```bash
pnpm install -g wrangler
wrangler login
wrangler pages deploy dist --project-name=spa-website
```

#### 2. 域名配置
在 Cloudflare DNS 設定:
```
A    polung.dpdns.org    → Cloudflare Pages IP
或
CNAME polung.dpdns.org  → your-project.pages.dev
```

#### 3. SSL/HTTPS 設定
- Cloudflare 自動提供免費 SSL 證書
- 啟用「Always Use HTTPS」
- 設定「Automatic HTTPS Rewrites」

---

### 階段五: 管理後台部署 (30分鐘)

#### 選項 1: 部署到 Cloudflare Pages
```bash
cd spa-admin
pnpm run build
wrangler pages deploy dist --project-name=spa-admin
```

訪問網址: `https://spa-admin.pages.dev`

#### 選項 2: 部署到 Firebase Hosting
```bash
pnpm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

訪問網址: `https://your-project.web.app/admin`

#### 選項 3: 部署到子域名
在 Cloudflare DNS 設定:
```
CNAME admin.polung.dpdns.org → spa-admin.pages.dev
```

---

## 🔒 安全性配置

### Firestore 安全規則

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 預約資料 - 任何人可讀寫(前端提交)
    match /appointments/{appointmentId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if request.auth != null; // 只有登入管理員可修改
    }
    
    // 療程服務 - 任何人可讀，只有管理員可寫
    match /services/{serviceId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // 設定 - 只有管理員可讀寫
    match /settings/{settingId} {
      allow read, write: if request.auth != null;
    }
    
    // 客戶資料 - 只有管理員可讀寫
    match /customers/{customerId} {
      allow read, write: if request.auth != null;
    }
  }
}
```

### Firebase Authentication 規則
- 只允許管理員 Email 登入
- 設定密碼強度要求
- 啟用雙因素認證(2FA) - 建議

---

## 💰 成本估算

### Firebase 免費方案 (Spark Plan)
- ✅ Firestore: 1GB 儲存 + 50K 讀取/天
- ✅ Authentication: 無限制
- ✅ Hosting: 10GB 流量/月
- ✅ Storage: 5GB 儲存

**預估:** 中小型SPA店完全足夠，**免費**

### Cloudflare Pages
- ✅ 無限帶寬
- ✅ 免費 SSL 證書
- ✅ 全球 CDN
- ✅ 500 次構建/月

**成本:** **免費**

### 域名成本
- 已有域名 polung.dpdns.org
- **成本:** **$0**

**總成本: $0/月** (在免費額度內)

---

## 📊 功能清單

### 前端網站
- [x] 線上預約表單
- [x] 預約確認頁面
- [x] 預約查詢功能
- [x] 服務項目展示
- [x] 聯絡表單
- [x] 多語言支援
- [x] 響應式設計

### 管理後台
- [x] 管理員登入
- [x] 預約列表(搜尋、篩選、排序)
- [x] 預約詳情查看
- [x] 預約狀態更新
- [x] 預約編輯/刪除
- [x] 客戶資料管理
- [x] 療程服務管理
- [x] 儀表板統計
- [x] 營業設定

---

## 📅 開發時程

| 階段 | 任務 | 預估時間 |
|------|------|----------|
| 1 | Firebase 項目設置 | 30分鐘 |
| 2 | 前端整合 Firebase | 2-3小時 |
| 3 | 開發預約表單 | 2-3小時 |
| 4 | 開發管理後台 | 4-6小時 |
| 5 | Cloudflare 配置 | 30分鐘 |
| 6 | 測試和優化 | 2-3小時 |
| **總計** | | **12-16小時** |

---

## 🎯 下一步行動

### 立即開始
1. ✅ 創建 Firebase 項目
2. ✅ 獲取 Firebase 配置
3. ✅ 整合到前端網站
4. ✅ 開發管理後台
5. ✅ 部署到 Cloudflare

### 需要的資訊
- [ ] Firebase 項目配置 (apiKey, projectId 等)
- [ ] 管理員 Email 和密碼
- [ ] GitHub repository 名稱
- [ ] Cloudflare 帳戶登入資訊

---

## 📚 相關文檔

### 官方文檔
- [Firebase 文檔](https://firebase.google.com/docs)
- [React Admin 文檔](https://marmelab.com/react-admin/)
- [Cloudflare Pages 文檔](https://developers.cloudflare.com/pages/)

### 教學資源
- Firebase Firestore 快速入門
- React Admin 完整教學
- Cloudflare Pages 部署指南

---

**文件創建時間:** 2025-10-12  
**最後更新:** 2025-10-12

